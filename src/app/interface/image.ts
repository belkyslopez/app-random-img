export interface Image {
  message: string;
  url:     string;
}

export interface ListImage {
  gif_count:   number;
  gifs:        string[];
  http:        string[];
  image_count: number;
  images:      string[];
}

